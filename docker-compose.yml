version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: shopee_manager_db
    restart: always
    environment:
      POSTGRES_USER: shopee_user
      POSTGRES_PASSWORD: shopee_password_2026
      POSTGRES_DB: estoque_shopee
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
